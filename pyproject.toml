[project]
name = "eterna"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "ruff>=0.16.6",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
